
#include<stdio.h>
#include<string.h>

    int main(){


        char str1[10],str2[10],str3[10],str4[10];
        int  number, len,len1, Row, colume,test_case=0;

            gets(str1);
            len  = strlen(str1);
            //printf("%d",len);
            for(row = 0, row<len; row++){
                for(colum = 0; colum<len; colum++){
                        if(row = colum){
                            test_case = 0;
                        }
                        if(str1[row]==str1[colum]){
                                i = 0;
                            str2[i] = str1[colum];
                                i++;
                        }

                }
                len1 = strlen(str2)
                for(j = 0, j<len1; j++){
                        for()
                if(str2[j]==str2[j+1]){

                }
                }
            }

    }
